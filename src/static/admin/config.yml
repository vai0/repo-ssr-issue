collections:
  - label: "Test"
    name: "test"
    extension: yml
    folder: "src/content"
    create: true
    identifier_field: slug
    fields:
      - label: name
        name: name
        widget: string
      - label: slug
        name: slug
        widget: string
      - label: title
        name: title
        widget: string
      - label: subtitle
        name: subtitle
        widget: string
      - label: summary
        name: summary
        widget: string

media_folder: "static/images/uploads"
public_folder: "/images/uploads"

backend:
  name: github
  repo: vai0/repo-ssr-issue